spring:
  application:
    name: trials
  datasource:
    url: jdbc:postgresql://localhost:5432/trialsdb?currentSchema=trials
    username: trialsusr
    password: Chang3Me!
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties.hibernate:
      hbm2ddl.auto: none
      jdbc.time_zone: UTC
      boot:
        allow_jdbc_metadata_access: false

  flyway:
    baseline-on-migrate: true
    enabled: false
    out-of-order: true

  mail:
    host: smtp-pulse.com
    port: 465
    properties.mail.smtp:
      starttls:
        enable: true
        required: true
      auth: true
      connectiontimeout: 5000
      timeout: 5000
      writetimeout: 5000
    username: secgj@htsjpuebla.gob.mx
    password: awZDQrinnb9pDhzzeYkV

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/trials-realm

  spring:
    servlet:
      multipart:
        max-file-size: 50MB
        max-request-size: 52MB

springdoc:
  swagger-ui:
    oauth:
      client-id: trials-service
      client-secret: 2CUuB2WvsBRRdBHUMMYgobGAm0QxhFDj

logging:
  level:
    org.keycloak.adapters.authorization.integration.jakarta.ServletPolicyEnforcerFilter: debug

server:
  port: 8888
  servlet:
    context-path: /rest

management:
  endpoints.web.exposure.include: "*"
  endpoint.health.show-details: always

keycloak:
  realm: trials-realm
  server-url: http://localhost:8080
  client-id: trials-service
  client-secret: 2CUuB2WvsBRRdBHUMMYgobGAm0QxhFDj
  client-server-id: 8c9e121a-53b2-45bf-89e0-8d1d03a1dd50

app:
  root-folder: /opt/pjp/files